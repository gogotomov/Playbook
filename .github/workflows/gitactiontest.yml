name: Github Action Test
on: push
jobs:
  my-job:
    name: GitHubAction
    runs-on: ubuntu-latest
    steps:
    - name: Clone GIT using Checkout@2 TESTTT123 action
      uses: actions/checkout@v1
      with:
        fetch-depth: 2
    - name: Diff
      run: |
        git diff HEAD^ HEAD
    - name: List files in GIT
      run: |
        cat Test1
